# Revision — run 01M32GNPT143Q82TJ217SNCC6A

- status reviewed: succeeded
- review: .fabro/reviews/develop/01M32GNPT143Q82TJ217SNCC6A.md
- seeds filed: none — 0 credit this pass, all surviving findings ride the overflow ledger
- balance: 0 non-exempt seeds filed / 0 — no credit this pass (no same-pass stale or superseded closes)
- basis: run 01M32GNPT143Q82TJ217SNCC6A, workflow version ff257a89b9734e9055b2a5c8fcfb380a69ba3f0819c5ec3d09ae9b6f09ab1918, commit 3e00c57326ba774d346835f911e8af45508a88e0
- revised_at_commit: 3e00c57326ba774d346835f911e8af45508a88e0 (ADR-0015: engine drift signal for later judgement)

## Findings

### Bound planner in-flight check with created_since on fabro_runs_list — already open in ledger

- overflow-dup: Bound planner in-flight fabro_runs_list with created_since=<now-48h> (open in 01M31EVYN6FD7CD6198J1J7XDS.md)
- Concrete change: mandate `created_since` (~24 h) on the `fabro_runs_list` call in `.fabro/workflows/develop/prompts/planner.md` step 4. Expected effect: ~$0.05 and 20–30 s saved per planning pass (this run: unfiltered call returned 140 runs, 44,948 input tokens / $0.0677 = 25% of run cost).

### Raise reviewer preamble_inline_max_kb from 16 to 24

- overflow: Raise reviewer preamble_inline_max_kb from 16 to 24 — in `.fabro/workflows/develop/workflow.fabro` reviewer node, change `preamble_inline_max_kb=16` to `24` so ~18 KB evidence captures render inline as the graph-level `preamble_budget_kb=48` comment intends; effect: removes one reviewer blob-detour tool path per review (this run: 18,984-byte capture blob-ref'd, reviewer paged `/tmp/sandbox-driver/runtime/blobs/4fea...json` with 3 tool calls and journaled the painpoint). Distinct from open evidence.nu overflows (blob collapsing 01M2ZYVQ7CBSY3T0FEYBE9JK74.md line 20; newline rendering 01M3201Q8GTC0EPT0S5YMK0Z32.md line 25) — this arm changes the render cap, not the capture.

### Flag human-only seeds deterministically in planner preflight

- overflow: Flag human-only seeds deterministically in planner preflight — add an arm to `.fabro/workflows/develop/scripts/planner-preflight.nu` marking candidates whose body begins "Deferred human follow-up" (the closeout deferred-action sweep's deterministic format; seeds-b56f and seeds-6eb8 match) as `needs_human: true`, plus one `.fabro/workflows/develop/prompts/planner.md` line to skip flagged candidates mechanically; effect: removes a recurring planner reasoning round on unimplementable seeds (this run's planner burned a full round plus a shell call on seeds-6eb8, still open atop `seeds ready`). Distinct mechanism from the open closeout-sweep classification overflow (01M32AAXM9Y4XDK3TFTREAZAMJ.md line 30): that fixes filing, this fixes preflight skipping.

### Declare output.planner in the planner node's context_allow_keys

- overflow: Declare output.planner in the planner node's context_allow_keys — in `.fabro/workflows/develop/workflow.fabro` planner node `context_allow_keys`, append `output.planner`; effect: warning-free planner completion (this run's worker log warned `context_update_dropped: context_allow_keys dropped: output.planner` at 17:38:42 — the engine's response-dedup writes that key every planner pass while the allow-key contract drops it).

### Quiet the .codex/instructions.md absence probe in worker logs

- overflow: Quiet the .codex/instructions.md absence probe in worker logs — add a `.codex/instructions.md` stub in this repo, or file a cross-repo demand (pattern of seeds-b291's INTEGRATION DEMANDS) for the engine to log the memory probe at debug when the file is absent; effect: error-level worker logs carry only actionable failures (this run: 6 ERROR lines, 2 per agent stage, all reporting `/workspace/seeds/.codex/instructions.md` not found while memory loads `AGENTS.md` fine).

### Fix the 'Usage: sd' label in the seeds CLI help surface

- overflow: Fix the 'Usage: sd' label in the seeds CLI help surface — in `crates/seeds` CLI help text, replace the inherited `Usage: sd <command> [options]` literal with `seeds` (or derive from argv[0]); `seeds prime` likewise still documents every command as `sd create`/`sd close`; effect: removes a standing confusion vector for agents keying on usage strings and for human users (observed on `seeds --help` by this run's implementer journal, confirmed on `seeds prime` by this revisor pass).
