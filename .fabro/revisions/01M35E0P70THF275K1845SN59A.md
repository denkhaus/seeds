# Revision — run 01M35E0P70THF275K1845SN59A

- status reviewed: succeeded
- review: .fabro/reviews/develop/01M35E0P70THF275K1845SN59A.md
- seeds filed: none — zero filing credit this pass (no same-pass stale/superseded closes; five open seeds cover different mechanisms), all six findings ride as overflow per ADR-0022
- balance: 0 non-exempt seeds filed / 0 — no credit this pass
- basis: run 01M35E0P70THF275K1845SN59A, workflow version e5dfb8c2b48eddbbf2dc1161498700bb6d7bf007d60c65b1c08e3ffb82cd3066, commit e2f1ed98b23ab9b798c13f701a09ea36d407e73e
- revised_at_commit: e2f1ed98b23ab9b798c13f701a09ea36d407e73e (ADR-0015: engine drift signal for later judgement)

## Findings

### Planner preflight: resolve anchor path-shorthand before flagging missing_file (priority 1)

- overflow-dup: planner-preflight anchor resolution (open in 01M30FZ05QPQNMTYPWHKPJBE8J.md, line 23, arm (a) — bare-path retry with crate-prefixed candidates). Concrete change from this run: on a repo-root miss in `.fabro/workflows/develop/scripts/planner-preflight.nu` (anchor arm, header lines 44–49), retry against `crates/*/tests/`, `crates/*/src/`, and `.fabro/**/` and emit the resolved path in `anchor_flags`. This run: 3 of 5 candidates (seeds-d30f, seeds-731d, seeds-7212) got false `missing_file` flags from path shorthand (e.g. `tests/sync.rs:86` vs real `crates/seeds/tests/sync.rs`); the planner burned 3 LLM rounds + 2 shell probes (~37s, ~$0.06–0.08) adjudicating them. Expected effect: flag churn drops to real rot only; no planner mis-close of a seed as stale-basis off a false flag.

### Keep mid-size evidence captures inline for the reviewer (priority 2)

- overflow-dup: raise reviewer `preamble_inline_max_kb` (open in 01M33WZF2KMHMP9W8RPJ0SB5X2.md, line 16 — same mechanism, 16→32; this run's capture is larger). Concrete change from this run: in `.fabro/workflows/develop/scripts/evidence.nu` cap/normalize individual diff line length for long-line fixtures, or raise `preamble_inline_max_kb` (16) in `.fabro/workflows/develop/workflow.fabro` so a ~60 KB capture renders inline. This run: the 53.4 KB capture (14 files) was dominated by two fixture diffs with multi-KB single lines, fell out of the 48 KB preamble budget without nearing HARD_CAP, and the reviewer spent its only `read_file` call paging the blob. Expected effect: reviewer judges from inline context; approvals stop depending on successful paging. Orthogonal to open seeds-731d (HARD_CAP discounting — different mechanism).

### Implementer verify lane: apply fmt instead of checking it (priority 2)

- overflow: Implementer verify lane: apply fmt instead of checking it — in `scripts/verify.nu` line 129, the `implementer` lane should run `cargo fmt -p <crate>` (apply) instead of `--check`; the deterministic tester gate keeps `--check` as the enforcement point; effect: one saved verify cycle (~30–60s + one LLM round) per deleted-block edit (this run: deleting the multi-line push-gate block left rustfmt drift, the first `just verify implementer` failed at `fmt --check`, forcing `cargo fmt` plus a full re-run — one wasted warm cycle + LLM round, implementer journal obs. 2); gate guarantee unchanged. No open seed or open overflow covers the verify-lane fmt mode (open verify.nu overflows are touched-path derivation, output truncation, parse tier, prompt-lint tier, rebuild-before-nextest — all different mechanisms).

### Briefs: make zero-hit sweep criteria authoritative over file inventories (priority 2)

- overflow: Briefs: make zero-hit sweep criteria authoritative over file inventories — in `.fabro/workflows/develop/prompts/planner.md` step 6/7, when acceptance criteria include a zero-hit sweep, the brief must carry one bullet stating the sweep result is authoritative over the seed's file inventory, with explicit exceptions enumerated; effect: purge-style seeds stop under-specifying scope and reviewer ping-pong on files outside the inventory ends (this run, seeds-d30f, first purge-class seed on this line: the named inventory missed `crates/seeds/src/args.rs:139` — leaving it would have kept `--force` accepted, a real acceptance hole — and `sd-reference/README.md:8`; only the `rg -in fabro` zero-hit sweep caught them, diff touched 14 files vs ~10 named). No open seed or open overflow covers sweep-authority brief wording (open planner.md overflows are token bounding, probe discipline, claim verification, publish_blocked legend — different mechanisms).

### Declare output.planner in the planner node's context_allow_keys (priority 2)

- overflow-dup: Add output.planner to planner context_allow_keys (open in 01M34N0KNQSGJQVXN1A6EDTJG1.md, line 26 — identical finding, still unconsumed). Concrete change: add `output.planner` to the planner node's `context_allow_keys` in `.fabro/workflows/develop/workflow.fabro`. This run: run.notice `context_update_dropped: output.planner` fired at planner completion (seq 90). Expected effect: recurring warn notice gone; the fabro-e47c/fabro-900e producer-declaration lint stays trustworthy.

### Gate lint: silence by-design routing-named and date-pin warnings (priority 2)

- overflow-dup: suppress by-design routing-named schema warnings and date-pin marker (open in 01M324AZVMHDSCSAMVP4WTZR1R.md, line 26 — same theme, `.fabro/scripts/prompt-lint.nu` mechanism). Concrete change from this run: whitelist routing-named top-level properties for `@schemas/*-output.schema.json` (planner-output and develop-output deliberately carry routing fields per fabro-9ec3 arm 3) and honor a load-bearing marker for the `2026-04-14` date pin, in the lint pass dispatched from `scripts/qualitygate.nu`. This run: the green gate tail carried 11 warnings (10× routing-named plus the date pin). Expected effect: the gate tail shows only actionable warnings.
