# Revision — run 01M359KDC33XMR46XMW18QXT67

- status reviewed: succeeded
- review: .fabro/reviews/develop/01M359KDC33XMR46XMW18QXT67.md
- seeds filed: none — zero filing credit this pass (no same-pass stale/superseded closes)
- balance: 0 non-exempt seeds filed / 0 — no credit this pass
- basis: run 01M359KDC33XMR46XMW18QXT67, workflow version c36202f76e5cffc6c58c5fe57fe4d7f8b8b2d1850ae3020b55472aba5e1769ad, commit 147aab68d439ef47fda8b4492a4fc8fa8ccc45e5
- revised_at_commit: 147aab68d439ef47fda8b4492a4fc8fa8ccc45e5 (ADR-0015: engine drift signal for later judgement)

## Findings

All three findings duplicate already-OPEN overflow-ledger entries (fabro-552a
rule 3: same theme already open → link line, no new `- overflow:` entry, no
double journaling). Zero-credit pass per ADR-0022 — nothing dropped, nothing
filed; the open entries stay the machine-visible record.

1. Add a graph-parse arm to the implementer's verify dispatch for
   workflow-graph diffs — run `dot -Tcanon` per touched
   `.fabro/workflows/**/*.fabro` graph in `scripts/verify.nu`; effect:
   sub-second in-pass syntax verification instead of a multi-minute
   gate-red/gatebounce cycle.
   - overflow-dup: Catch workflow.fabro parse errors in the implementer
     verify lane (open in 01M33WZF2KMHMP9W8RPJ0SB5X2.md, line 13)

2. Render the preflight table once in the planner prompt via
   `preamble_stages_ignore` — add `preflight` to the planner node's
   `preamble_stages_ignore` in `.fabro/workflows/develop/workflow.fabro`;
   effect: ~300 fewer tokens per planner prompt and no two-copy drift trap.
   - overflow-dup: Stop rendering preflight JSON twice in preambles (open in
     01M31Z2XTPMMA78KHR4D9K84E6.md, line 28 — already covers the planner node
     and additionally `tracker_guard`/`claim_check` and the implementer)

3. Silence the recurring `context_update_dropped` warn for the planner's own
   dedup key — add `output.planner` to the planner node's
   `context_allow_keys` in `.fabro/workflows/develop/workflow.fabro`;
   effect: one fewer warn per run, warn channel stays meaningful.
   - overflow-dup: Add output.planner to planner context_allow_keys (open in
     01M34N0KNQSGJQVXN1A6EDTJG1.md, line 26)
