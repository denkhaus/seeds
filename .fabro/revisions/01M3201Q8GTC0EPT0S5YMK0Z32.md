# Revision — run 01M3201Q8GTC0EPT0S5YMK0Z32

- status reviewed: succeeded
- review: .fabro/reviews/develop/01M3201Q8GTC0EPT0S5YMK0Z32.md
- seeds filed: none — zero balance credit this pass (no same-pass stale/superseded closes), all surviving findings journaled as overflow below
- balance: 0 / 0 — no credit this pass
- basis: run 01M3201Q8GTC0EPT0S5YMK0Z32, workflow version e3c92fec9a9a6fd9256277d60e06e12dddcacf6779280c9df5c2b2825b813ea8, commit 7243a1a2ee84d8b415f1716b1273d05ca60cec46
- revised_at_commit: 7243a1a2ee84d8b415f1716b1273d05ca60cec46 (ADR-0015: engine drift signal for later judgement)

## Findings

### dup-run-check: match seed ids in subject and trailer lines only, never full commit bodies
- filed id: none — overflow to journal (no credit this pass)
- Concrete change: in `.fabro/scripts/dup-run-check.nu`, restrict the seed-id match to the commit subject line and `Fabro-Run:`/trailer lines. Expected effect: false-positive duplicate verdicts (run 01M3201Q8GTC0EPT0S5YMK0Z32 flagged `seeds-facc` via `e307edf` prose-only body mention, ~40s planner adjudication, implementer override) disappear; the mechanical Blocked-on-duplicate route becomes safe. Not covered by seeds-69ae (closed, default-base fix) or seeds-b291 (open, tracker-state heal). Related but distinct from the open dup-run-check ref-derivation overflow in `01M2ZYVQ7CBSY3T0FEYBE9JK74.md` line 19 — different mechanism.
- overflow: dup-run-check: match seed ids in subject and trailer lines only, never full commit bodies — restrict the seed-id match in `.fabro/scripts/dup-run-check.nu` to commit subjects and `Fabro-Run:`/trailer lines; effect: false-positive duplicate verdicts disappear (run 01M3201Q8GTC0EPT0S5YMK0Z32) and the mechanical Blocked route becomes safe.

### verify.nu: derive touched paths from git status --porcelain so untracked files count
- filed id: none — overflow to journal (no credit this pass)
- Concrete change: in `scripts/verify.nu`, derive touched paths from `git status --porcelain` or the union with `git diff` so `??` entries count. Expected effect: the test-file-touched class triggers the full-suite lane mechanically (this run's untracked `tests/cli.rs` was invisible, lane skipped, 47-test suite hand-run). Distinct from the open touched-crate detection overflow in `01M2ZYVQ7CBSY3T0FEYBE9JK74.md` line 14 (crates regex, seeds-9482 class) — untracked blindness is a different defect class.
- overflow: verify.nu: derive touched paths from git status --porcelain so untracked files count — union `git status --porcelain` with diff-derived paths in `scripts/verify.nu` so `??` entries count; effect: test-file-touched triggers the full-suite lane mechanically, no manual compensation.

### evidence.nu: emit capture with real newlines so read_file paging works
- filed id: none — overflow to journal (no credit this pass)
- Concrete change: in `.fabro/workflows/develop/scripts/evidence.nu`, emit the capture pretty-printed or one diff hunk per line instead of a single-line JSON string. Expected effect: reviewers page any capture size directly (this run's 96.2 KB single-line blob defeated line-based read_file; reviewer fell back to a repo read that is Verification-blocked on a dirty tree).
- overflow: evidence.nu: emit capture with real newlines so read_file paging works — pretty-print or emit one diff hunk per line in `.fabro/workflows/develop/scripts/evidence.nu` instead of a single-line JSON string; effect: reviewers page any capture size directly, eliminating the repo-read fallback and Verification-bounce class.

### Lesson capture: init .mulch for this repo or make the implementer prompt degrade cleanly
- overflow-dup: Init mulch or drop the ml mandate in AGENTS.md (open in `01M2ZYVQ7CBSY3T0FEYBE9JK74.md`)
- Same theme already open in the ledger (line 18 of `01M2ZYVQ7CBSY3T0FEYBE9JK74.md`); this pass adds fresh evidence: run 01M3201Q8GTC0EPT0S5YMK0Z32 lost a durable finding (sd 0.5.15 exits 1 on all error paths) to `No .mulch/ directory found`, and the revisor's own `ml prime` failed identically. The open entry already names both fix directions (`mulch init` as a loop-asset seed, or degrading the implementer prompt's Lesson-capture section).

### Planner: probe-verify reference-behavior claims before they enter the brief
- filed id: none — overflow to journal (no credit this pass)
- Concrete change: in `.fabro/workflows/develop/prompts/planner.md` step 7, add that any brief bullet asserting observable runtime behavior of a named reference tool must be settled by the cheapest live probe at plan time or explicitly labeled 'unverified claim (spec-transcribed)'. Expected effect: stale behavioral claims stop reaching the implementer (this run transcribed a stale exit-0 claim, implementer disproved it via live sd probes and shipped a MATERIAL SPEC CORRECTION deviation, reviewer had to adjudicate brief-vs-code divergence).
- overflow: Planner: probe-verify reference-behavior claims before they enter the brief — amend `.fabro/workflows/develop/prompts/planner.md` step 7 to require a live probe or an explicit 'unverified claim (spec-transcribed)' label for reference-tool behavior bullets; effect: stale behavioral claims stop reaching the implementer, removing mid-run spec corrections and reviewer adjudication tax.
