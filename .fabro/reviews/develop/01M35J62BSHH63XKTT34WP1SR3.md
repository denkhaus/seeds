# Improve review — run 01M35J62BSHH63XKTT34WP1SR3

- workflow: develop
- branch integrated: this revisor pass (unmerged until approved)
- status: succeeded (4.4 min, revisor pass — reason and cost in run detail)
- generated: 2026-09-22 22:10+0000 by revisor `fabro_ask`

---

All recommendations below are grounded in this specific run (`01M35J62BSHH63XKTT34WP1SR3`, seed `seeds-e5af`, PR #55 — a clean first-pass cycle: 10/10 stages green, 4m25s wall, $0.240 total; planner 44.6s/$0.048, implementer 152s/$0.168, reviewer 22.4s/$0.024, gate green in 3.3s). Open seeds in the tracker today: `seeds-731d`, `seeds-7212`, `seeds-7e0f` (from `.seeds/issues.jsonl`).

---

**1. Reviewer approved with the evidence capture's anomaly section cut mid-file — raise the reviewer's line cap.**
From run events: the reviewer's preamble rendered the 12.3 KB evidence capture but ended "(14 lines omitted)" — the tail of the `AGENTS.md` diff and the entire `README.md` diff in the "changed files NOT named by the seed spec" section, the exact section the reviewer prompt says must be adjudicated file-by-file. The reviewer made **0 tool calls** (never paged the blob) and approved anyway. **Change:** `preamble_output_max_lines` 200 → 300 on the reviewer node in `.fabro/workflows/develop/workflow.fabro` (or bound the anomaly section in `evidence.nu` so it fits). **Expected effect:** mixed captures with 6+ loop-churn files render whole; approvals stop resting on unseen diffs. **Seed:** amend `seeds-731d` — same failure class ("every seed-work file reviewable"), but this is the *second* truncation point (the 200-line preamble cap, not the 128 KB `HARD_CAP`), so extend its scope rather than file a duplicate.

**2. Preflight anchor arm false-flags 3 of 4 candidates — teach it negative invariants and loop-asset path resolution.**
From run events (`output.preflight`): `seeds-e5af` flagged `lefthook/hooksPath/core.hooks missing_file` (an invariant the seed *wants* absent), `seeds-731d` flagged `evidence.nu:38` and `seeds-7212` flagged `workflow.fabro:399` — both bare filenames that actually live under `.fabro/workflows/develop/`. The planner then burned 4 of its 6 shell calls hand-adjudicating (ls/git-log-grep/grep/cat, events seq 57–71) — and `git log --grep` duplicated the preflight's own landed-ness arm. **Change:** in `.fabro/workflows/develop/scripts/planner-preflight.nu`, resolve bare filenames against the loop-asset dirs and support a `!`-prefix anchor syntax meaning "must NOT exist". **Expected effect:** `anchors_ok` stops crying wolf; ~10–15s and 3–4 planner tool calls saved per run, and the *next* runs claiming `seeds-731d`/`seeds-7212` don't re-hit the same two false flags. **New-seed justification:** no open seed covers the anchor checker's resolution/false-positive class (731d=evidence cap, 7212=reviewer prompt text, 7e0f=untracked derivation).

**3. New fixture batteries require a manual adjacent edit to the gate — auto-discover `*-fixtures.nu`.**
From the implementer journal and diff: wiring `push-gate-fixtures.nu` into the tester step required an "adjacent wiring" edit to the hardcoded `batteries` list in `scripts/qualitygate.nu` (lines ~153+), which then landed in the evidence anomaly section and consumed reviewer adjudication. This happens for every new battery. **Change:** in `scripts/qualitygate.nu`, glob-discover `.fabro/scripts/*-fixtures.nu` (the existing `*-fixtures.nu` convention already excludes tools like `stage-journal.nu`/`friction-score.nu`, so the "never blanket-run" comment's concern is respected). **Expected effect:** new batteries self-wire; one recurring adjacent loop-asset edit + anomaly-section adjudication disappears per battery-adding seed. **New-seed justification:** no existing seed covers battery wiring (7e0f is about *untracked-file* derivation in verify/qualitygate, a different arm of the same script).

**4. The implementer re-hit a nushell pitfall the loop had already recorded — surface nushell expertise as a skill.**
From run events + worker log: the implementer's 3 failed shell calls (and 16 `unsupported_control` provider warnings, 22:03:15–22:04:35) were the multiline-bare-argument parse failure, fixed in-session and recorded as `mx-d0eb14` in `.mulch/expertise/nushell.jsonl` — which now holds two records (`mx-d157ad` before it) that nothing feeds back to agents editing `.nu` files. Rust has a binding style-guide gate; nushell (27 repo scripts) has none. **Change:** add `.fabro/skills/nushell/SKILL.md` distilled from the expertise records (auto-discovered via the existing `skills="discover"`), plus one gate sentence in `.fabro/workflows/develop/prompts/implementer.md` mirroring the Rust rule: "when the seed touches `*.nu`, read the nushell skill first." **Expected effect:** the mx-d0eb14/d157ad error class stops recurring — this run paid ~3 wasted LLM rounds of the stage that is already 70% of run cost ($0.168/$0.240). **New-seed justification:** no seed covers nushell lesson surfacing (a0fa is fabro_ask; c760 was the reviewer-side skills residue, closed).

**5. Every planner pass emits a `context_update_dropped` WARN — declare the engine's dedup keys.**
From the worker log and event seq 85: `context_allow_keys dropped: output.planner` — the engine's response-dedup writes `output.planner`/`response.planner`, but the planner node's `context_allow_keys` declares only `current_seed_id,current_seed_title,current_seed_brief,review_verdict,journal`. **Change:** one line in `.fabro/workflows/develop/workflow.fabro` (planner node): add `output.planner,response.planner` to `context_allow_keys`. **Expected effect:** kills a recurring WARN on every planner stage and restores meaning to the fabro-900e producer-declaration lint the attr exists for. **New-seed justification:** the fabro-900e comments describe the declaration pattern but no seed tracks this specific mismatch.

**6. Prompt-lint prints 11 warnings on every gate run — 10 are false alarms, 1 is a genuinely stale pin.**
From the tester output: `prompt-lint: ok — 44 files, 11 warnings` — 5× "routing-named property" against `planner-output.schema.json`, 5× against `conductor/develop-output.schema.json` (both *intentionally* routing-shaped per fabro-9ec3/0a4c), plus the real one: `project-facts.md: date pin '2026-04-14' older than 45 days — still load-bearing?`. **Change:** in `.fabro/scripts/prompt-lint.nu`, allowlist routing-named properties for schemas under `.fabro/workflows/*/schemas/` (they are routing contracts by design), and support a `pinned-until:` acknowledgment marker for the toolchain date pin (an actual bump is an operator/image action, per the seeds-6eb8 precedent). **Expected effect:** warnings drop 11 → 0 on a healthy tree, so the next *real* warning is visible in gate output instead of drowned. **New-seed justification:** no seed covers lint-noise reduction or the date-pin acknowledgment.

**7. Engine/provider noise polluted this run's diagnostics — file a handoff seed against the fabro repo.**
From the worker log: 6× ERROR `"/workspace/seeds/.codex/instructions.md" was not found` (2 per agent-session init × 3 sessions), 16× WARN `unsupported_control ... tool result error flag` (zai protocol, already a known-noise candidate), and one PR-body generation failure (`PR content structured generation failed; retrying once without strict JSON`, 22:05:23 — the glm-4.7 PR model returned no JSON; the retry recovered). None broke the run, but all three recur every run and bury real signals in ERROR/WARN streams. **Change:** file a handoff-type seed (precedent: `seeds-800d`) directing the operator to push the fabro-repo follow-ups: skip the `.codex` probe quietly, add `unsupported_control` to the zai restraint list, make non-strict-JSON the default fallback for PR content generation. **Expected effect:** worker logs become triageable; PR-body generation stops depending on a silent retry. **New-seed justification:** these are engine-side (denkhaus/fabro) surfaces — no seeds-side seed covers them, and the seeds-800d precedent shows the correct vehicle is a handoff seed in this tracker.

---

Not recommended despite being visible: the implementer re-running `dup-run-check` although the preflight verdict was inline in its own preamble — that duplication is deliberate defense-in-depth per the prompt's claim-race rationale and cost ~1s here; and the `output.gatebounce absent` WARN is by-design (the key only exists after a gate-red bounce this run never had).
