# Improve review — run 01M359KDC33XMR46XMW18QXT67

- workflow: develop
- branch integrated: this revisor pass (unmerged until approved)
- status: succeeded (3.2 min, revisor pass — reason and cost in run detail)
- generated: 2026-09-22 19:40+0000 by revisor `fabro_ask`

---

## What this run actually did (grounding)

From run events: run `01M359KDC33XMR46XMW18QXT67` claimed `seeds-a0fa` (add `fabro_ask` to planner+implementer `fabro_tools`), all 10 stages first-pass green in 165s wall / $0.2406, PR #49. Stage split: planner 52.3s / **$0.139 (58% of run cost)**, implementer 74.2s wall (72.8s inference, 1.05s tool) / $0.080, reviewer 13.6s / $0.021 (**zero tool calls** — judged entirely from the inline 15.6KB evidence capture; that pipe worked, don't touch it), tester gate 3.35s ("no crates touched" — correctly tiered), all deterministic nodes ≤1.8s. Two open seeds exist in the tracker: `seeds-37bc` and `seeds-800d`; everything else is closed.

## Recommendations, by expected impact

**1. Bound the planner's `fabro_runs_list` in-flight call — the single biggest cost/latency item in this run.**
Evidence (run events seq 54–55): the planner's `fabro_runs_list` call took **19.0s** (19:31:27.24→19:31:46.23) and returned **163 runs (~50KB)** to answer a yes/no question ("is any other run claiming seeds-a0fa?" — answer: none). The planner's conversation jumped from 7,959 → 59,486 tokens; the three LLM rounds carrying that dump cost $0.0768 + $0.0200 + $0.0207 = **$0.117 of the planner's $0.139 (49% of total run cost)**.
Change: implement the **in-repo arm of `seeds-37bc`** — `.fabro/workflows/develop/prompts/planner.md` step 4: call `fabro_runs_list` with `created_since=<now-48h>` and instruct the planner to consider only non-terminal/open-PR rows; the engine-side filter params are the seed's engine half. `seeds-37bc` is open, P1, unassigned, and its own body prescribes exactly this fallback when denkhaus/fabro is out of reach.
Expected effect: planner context stays ~13k tokens instead of 66k; ~$0.07–0.12 and ~19s recovered per develop run; cache prefix preserved.

**2. Give the implementer lane a graph-parse check for config-only workflow seeds.**
Evidence: implementer@1 journal (from run events/checkpoint diff) — it *tried* `nu .fabro/scripts/parse-workflow.nu`, which doesn't exist (confirmed via workspace listing of `.fabro/scripts/`), then `just verify implementer` answered "no lib/ crates touched — nothing to verify". Graph syntax was checked **only** at the tester ("workflow graphs parse (graphviz)" arm in the 3.35s gate). A DOT typo would have surfaced as a gate-red → gatebounce → implementer cycle instead of an in-pass fix.
Change: add a loop-asset arm to `scripts/verify.nu` (its `touched`/dispatch, lines ~70–80, currently derives only `lib/`+`crates/` paths): when the diff touches `.fabro/workflows/**/*.fabro`, run the same `dot -Tcanon` parse the gate runs, per touched graph.
Expected effect: sub-second in-pass syntax verification; typo-class failures fixed by the implementer in the same pass instead of a multi-minute gate-bounce cycle (the prompts' own lore prices each wasted implementer recovery at ~50s).
New-seed justification: no open seed covers implementer-lane graph verification (checked tracker: 37bc = runs_list filtering, 800d = conductor gate repair); the implementer journal of *this run* is the filing evidence.

**3. Stop rendering the preflight table twice in the planner prompt.**
Evidence: the planner@1 stage prompt (seq 35) carries the full ~1.1KB preflight JSON **twice** — once in "## Completed stages" (script output) and once in "## Context" as `output.preflight` (delared via `context_allow_keys`).
Change: `.fabro/workflows/develop/workflow.fabro`, planner node — add `preflight` to `preamble_stages_ignore` (currently `"planner,implementer,evidence"`); the table still arrives via the context key, which the prompt already says to read first.
Expected effect: ~1.1KB (~300 tokens) less per planner prompt per visit, and removes a drift trap where the two copies can diverge under budget truncation. Small dollars; ranked accordingly.
New-seed justification: the graph's existing "residual duplication" comment covers per-visit *engine-side* stage sections; no seed covers this graph-side single-visit duplication.

**4. Silence the recurring `context_update_dropped` warn for the planner's own dedup key.**
Evidence: run.notice seq 78, level=warn, `context_allow_keys dropped: output.planner` — the engine's response-dedup writes `output.planner`, which the planner's strict `context_allow_keys` (fabro-900e lint) then drops with a warning on **every** planner pass (the data itself survives in checkpoints; only the warn is noise). Operator monitoring greps severity warn/ERROR (handoff seeds-7cd1 recipe), so warn-channel noise has real cost.
Change: `.fabro/workflows/develop/workflow.fabro`, planner node — add `output.planner` to `context_allow_keys` (mirroring how the implementer declares its own output keys).
Expected effect: one fewer warn per run; warn channel stays meaningful for genuine contract drift.
New-seed justification: no open seed covers engine-dedup-key lint hygiene; evidence is this run's seq 78 notice.

**Explicitly not recommended:** the reviewer setup (zero tools, first-pass approval from the inline evidence capture — `preamble_inline_max_kb=16`/200-line cap did their job), the gate's crate-tiering, and the guard/preflight/claim_check/closeout script nodes (85ms–1.8s each) all performed as designed in this run; changing them now would be ungrounded churn.
