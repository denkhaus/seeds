# Improve review — run 01M35E0P70THF275K1845SN59A

- workflow: develop
- branch integrated: this revisor pass (unmerged until approved)
- status: succeeded (6.1 min, revisor pass — reason and cost in run detail)
- generated: 2026-09-22 21:00+0000 by revisor `fabro_ask`

---

All evidence gathered — run events, stage transcripts/timings/cost, the journal, and the tracker (open seeds: seeds-37bc, seeds-8795, seeds-7e0f, seeds-7212, seeds-731d). This was a clean single-cycle run (seeds-d30f, approved+closed, PR #51): 5m35s wall, **$0.531 total** — planner $0.204 (38%), implementer $0.282 (53%), reviewer $0.045; gate green in 8.9s (warm cache — the verify-lane design worked). Recommendations, ordered by expected impact:

---

**1. Skip/bound the planner's `fabro_runs_list` call — seed seeds-8795** (engine arm: seeds-37bc)
*What happened:* the preflight table already said `in_flight:false` for **all five** candidates, yet the planner called `fabro_runs_list {workflow:develop}` unbounded (seq 48–49): 20.0s tool time, 164 runs dumped raw, conversation tokens jumped 8,654 → 59,762, and the very next LLM round alone cost **$0.077 (~15% of run cost)**. Third recorded recurrence.
*Change:* `.fabro/workflows/develop/prompts/planner.md` step 4 — skip the call when the preflight marks all top candidates `in_flight:false`; otherwise pass `created_since=<now-48h>`.
*Effect:* ~$0.10 + 30–40s recovered per develop run; planner prefix cache preserved.

**2. Resolve anchor false-positives mechanically in the preflight — NEW SEED**
*Justification:* no open or closed seed covers anchor *fallback resolution* (seeds-731d/7212/7e0f/8795/37bc are all other mechanisms).
*What happened:* 3 of 5 candidates got `missing_file` anchor flags that were pure path-shorthand false positives — seeds-d30f cited `tests/sync.rs:86` (real: `crates/seeds/tests/sync.rs`), seeds-731d cited `evidence.nu:38` (real: `.fabro/workflows/develop/scripts/evidence.nu`), seeds-7212 cited `workflow.fabro:399`. The planner burned 3 LLM rounds + 2 shell probes (~37s, ~$0.06–0.08, seq 53–78) adjudicating and correcting via `seeds update`.
*Change:* `.fabro/workflows/develop/scripts/planner-preflight.nu` anchor arm (fabro-7daf/9ec3 arm 1, header lines 44–49): on a repo-root miss, retry against `crates/*/tests/`, `crates/*/src/`, and `.fabro/**/` before flagging; emit the resolved path in `anchor_flags`.
*Effect:* flag churn drops to real rot only; ~30s/$0.05 per affected run; removes the risk of a planner mis-closing a seed as stale-basis off a false `missing_file`.

**3. Keep mid-size evidence captures inline — seed seeds-731d (extend its scope)**
*What happened:* the capture was **53.4 KB** (evidence@1, 14 files) — over the 48 KB preamble budget *without* nearing HARD_CAP — dominated by two fixture diffs whose single lines are multi-KB (`repo_issues_sd_list.json`, `repo_seeds/issues.jsonl`). The reviewer got a blob ref and spent its only `read_file` call paging it (reviewer journal obs. 1).
*Change:* `.fabro/workflows/develop/scripts/evidence.nu` (per seeds-731d) plus one addition this run motivates: cap/normalize individual diff **line length** for long-line fixtures, or raise the reviewer node's `preamble_inline_max_kb` (16) in `workflow.fabro` so a ~60 KB capture renders inline.
*Effect:* reviewer judges from inline context; no blob round-trip; approvals stop depending on successful paging.

**4. Apply fmt in the implementer verify lane — NEW SEED**
*Justification:* no existing seed touches verify.nu's fmt step (seeds-7e0f covers untracked-file *derivation*, not formatting).
*What happened:* deleting the multi-line gate block left rustfmt drift; the first `just verify implementer` failed at `fmt --check` (verify.nu:129), forcing a `cargo fmt` + full re-run — one wasted warm cycle + LLM round (implementer journal obs. 2).
*Change:* `scripts/verify.nu` line 129 — in the `implementer` lane, run `cargo fmt -p <crate>` (apply) instead of `--check`; the deterministic tester gate keeps `--check` as the enforcement point.
*Effect:* one saved verify cycle (~30–60s + one LLM round) per deleted-block edit; gate guarantees unchanged.

**5. Make grep-sweep criteria authoritative over file inventories in briefs — NEW SEED**
*Justification:* no seed covers brief-construction policy for sweep-style seeds; this is the first purge-class seed on this line.
*What happened:* the seed's named inventory missed two criterion-reachable sites — `crates/seeds/src/args.rs:139` (leaving the SYNC_SPEC force flag would have kept `--force` accepted, i.e. a real acceptance hole) and `sd-reference/README.md:8`; only the `rg -in fabro` zero-hit sweep caught them (implementer journal obs. 1).
*Change:* `.fabro/workflows/develop/prompts/planner.md` step 6/7 — when acceptance criteria include a zero-hit sweep, the brief must carry one bullet: "the sweep result is authoritative over the inventory; enumerate explicit exceptions."
*Effect:* future purge seeds stop under-specifying scope; removes reviewer ping-pong risk on "files outside the inventory" (this diff touched 14 files vs ~10 named).

**6. Declare `output.planner` in the planner's context_allow_keys — NEW SEED**
*Justification:* config-drift fix; no seed covers it (checked tracker for `context_allow_keys`/`output.planner`).
*What happened:* run.notice `context_update_dropped: output.planner` fired at planner completion (seq 90) — the engine's own dedup record key isn't in the node's declared contract (`current_seed_id,…,journal` in `workflow.fabro`).
*Change:* add `output.planner` to the planner node's `context_allow_keys` in `.fabro/workflows/develop/workflow.fabro`.
*Effect:* recurring warn notice gone; the fabro-e47c/fabro-900e producer-declaration lint stays trustworthy.

**7. Silence by-design lint warnings in the gate tail — NEW SEED**
*Justification:* no seed covers the loop-asset lint's false positives; they are not the residual-sweep class seeds-81cd closed.
*What happened:* the green gate output carries **11 warnings every run** — 10× "top-level property … is routing-named" against schemas that *deliberately* carry routing fields (planner-output.schema.json per fabro-9ec3 arm 3, conductor develop-output.schema.json), plus the recurring date-pin warning on `2026-04-14`.
*Change:* the loop-asset lint pass dispatched from `scripts/qualitygate.nu` — whitelist routing-named properties for `@schemas/*-output.schema.json` and honor a load-bearing marker for date pins.
*Effect:* the gate tail (the one output humans and bounce-cycle agents read) shows only actionable warnings.

---

Could not inspect: the internals of the lint pass inside `scripts/qualitygate.nu` (recommendation 7 names it by its output rather than a verified line number), and whether the implementer read the Rust style guide via shell (19 shell calls are aggregated; its `use_skill` was never invoked, but the prompt prescribes a shell read — no violation is provable either way). Positively worth noting: seeds-7212's feared gap did **not** recur — this run's reviewer loaded `rust-style-guide` via `use_skill` and approved in 32.5s/$0.045; that seed remains valid but is now lower priority.
