# Improve review — run 01M32GNPT143Q82TJ217SNCC6A

- workflow: develop
- branch integrated: this revisor pass (unmerged until approved)
- status: succeeded (3.7 min, revisor pass — reason and cost in run detail)
- generated: 2026-09-21 17:48+0000 by revisor `fabro_ask`

---

All evidence gathered — run events (stage timings, usage, transcripts), stage journals, worker log, and the full tracker (13 seeds, `.seeds/issues.jsonl`). Here is the run-grounded assessment.

## Run baseline (from run events / conclusion)

First-pass green, 0 retries: **200.9 s wall, $0.2712 total**. Split: planner **96.0 s / $0.1847 (68% of cost, ~48% of active wall)**, implementer 50.9 s / $0.0541, reviewer 29.7 s / $0.0324, all script nodes < 2 s. The implementer was exemplary (one mechanical `sed` pass, 50 s inference vs 0.9 s tool time); the recoverable waste sits in the planner and the evidence pipe.

## Recommendations, by expected impact

**1. Bound the planner's `fabro_runs_list` call with `created_since`** — *prompting / tool usage*
What happened: planner called it unfiltered (`{workflow: "develop"}`, run events seq 48); it returned **140 runs** (~17.2 s tool call), and the LLM round digesting it (seq 52) jumped to 44,948 input tokens / **$0.0677 — 25% of the entire run's cost in one round**. The tool already supports `created_since` (per its own description); the planner prompt never uses it. In-flight protection only needs recent runs — older non-terminal ones are the tracker_guard's stale-claim territory (6 h threshold).
Change: `.fabro/workflows/develop/prompts/planner.md`, step 4 (IN-FLIGHT EXCLUSION) — mandate `created_since` (~24 h) on the `fabro_runs_list` call.
Effect: cuts the dump from 140 runs to a handful; saves roughly $0.05+ and 20–30 s on every planning pass.
*New-seed justification: no tracker seed mentions `fabro_runs_list`, `created_since`, or in-flight-check cost — closest lore (fabro-06e0/fabro-91ff) lives in the origin repo's tracker, not this one.*

**2. Raise the reviewer's `preamble_inline_max_kb` 16 → 24** — *graph design*
What happened: the 18,984-byte evidence capture (run events, evidence@1 `output_bytes`) exceeded the reviewer node's per-value cap of 16 KB and blob-ref'd, despite the graph-level `preamble_budget_kb=48` whose own comment (fabro-1e9f, in `workflow.fabro`) says ~18 KB captures "must render inline, zero blob detours". The reviewer had to page `/tmp/sandbox-driver/runtime/blobs/4fea...json` manually (3 tool calls) and filed this exact painpoint in its journal: "the preamble budget appears to cut evidence captures far too eagerly".
Change: `.fabro/workflows/develop/workflow.fabro`, reviewer node, `preamble_inline_max_kb=16` → `24`.
Effect: evidence renders inline as the 48 KB budget already intends; removes one tool detour per review and the "unread blob ref" failure mode that can force a Verification-blocked re-capture cycle.
*New-seed justification: the seeds that set these knobs (fabro-9467, fabro-1e9f) are origin-repo ids; this repo's tracker has no seed for the inline cap, and the contradiction between the two settings is first observed here.*

**3. Deterministic skip for human-only follow-up seeds** — *graph design / prompting*
What happened: `seeds ready` lists **seeds-6eb8** (deferred *human* operator task: docker host, ghcr push, server-side repin) at the top; the planner burned a full reasoning round plus a shell call assessing and skipping it, and journaled: "it sits assigned to fabro at the top of seeds ready and risks burning a cycle." It's still open, so every future run re-derives this skip — and a weaker pass could claim it and waste a whole cycle.
Change: add an arm to `.fabro/workflows/develop/scripts/planner-preflight.nu` that flags candidates whose body begins "Deferred human follow-up" (the closeout deferred-action sweep's deterministic format — seeds-b56f and seeds-6eb8 both match it) as `needs_human: true`, plus one planner.md line to skip flagged candidates mechanically. (Zero-code alternative: the user unassigns seeds-6eb8, per the planner's own suggestion.)
Effect: removes a recurring planning round and eliminates the wasted-cycle risk on an unimplementable seed.
*New-seed justification: no existing seed covers preflight triage of human-only seeds; seeds-6eb8 itself is the symptom, not the workflow fix.*

**4. Implement seeds-9fa3 (reviewer style-guide access) before the next Rust seed runs** — *known seed: seeds-9fa3*
What happened this run: the reviewer session lists `rust-style-guide` under *available skills* but its tool allow-list (`read_file,grep,glob`) admits no `use_skill` — the skill is advertised yet uninvocable (`activated: []`, run events reviewer@1). Harmless here (loop-asset-only diff), but the next open seed **seeds-436a** touches `crates/seeds/src/render.rs` (Rust), where the reviewer prompt's binding "read the guide before judging" is currently unenforceable.
Change: implement seeds-9fa3 (already user-APPROVED 2026-09-21, both fix directions pre-cleared) in `.fabro/workflows/develop/workflow.fabro` — direction (a) `use_skill` in the reviewer tools list or (b) fs_hide exemption for `.fabro/skills/**`.
Effect: the Rust standards axis becomes mechanically loadable exactly when seeds-436a's review needs it.

**5. Declare `output.planner` in the planner's `context_allow_keys`** — *error handling*
What happened: run notice + worker log warn at 17:38:42 — `context_update_dropped: context_allow_keys dropped: output.planner`. The engine's response-dedup writes `output.planner` every planner pass; the planner's fabro-e900e allow-key contract then drops it, emitting a warning per run.
Change: `.fabro/workflows/develop/workflow.fabro`, planner node `context_allow_keys` — append `output.planner` (or suppress the dedup key generation).
Effect: warning-free stage completion; the declared producer contract matches actual engine behavior.
*New-seed justification: contract-hygiene nit first surfaced in this run's log; no tracker seed covers context_allow_keys drift on the planner.*

**6. Quiet the `.codex/instructions.md` absence probe** — *error handling*
What happened: the worker log shows **6 ERROR-level lines per run** (2 × each agent stage: planner, implementer, reviewer) — all `File "/workspace/seeds/.codex/instructions.md" was not found`. Harmless (memory correctly loads `AGENTS.md`), but it pollutes the error channel that should carry real failures.
Change: file a cross-repo demand seed (pattern of seeds-b291's "INTEGRATION DEMANDS") for the engine to log that probe at debug when the file is absent — or add a `.codex/instructions.md` stub in this repo.
Effect: error-level logs carry only actionable failures; cheaper incident triage.
*New-seed justification: no existing seed touches the codex-profile memory probe.*

**7. Fix the `Usage: sd` label in the seeds CLI help text** — *UX*
What happened: the implementer's journal observation this run — `seeds --help` prints "Usage: sd <command> [options]" (inherited argv0 label, visible in the planner's own shell output too). Any future prompt keyed on usage strings, and every human user, sees the wrong binary name.
Change: `crates/seeds` help text (the usage literal in the CLI help surface) — derive from argv[0] or hardcode `seeds`.
Effect: removes a standing confusion vector for both agents and users; trivial, one-line-class fix.
*New-seed justification: seeds-facc covered flag parity, not the help-text label; no open seed names it.*

**Not recommended (measured healthy this run):** gate fast-path (3.1 s green on "no crates touched" — correct, the diff was `.fabro/`-only, and seeds-9482 already fixed the no-op detection class), implementer discipline (chained recon, mechanical transform, 0.9 s tool time), tracker_guard/preflight chain (< 2 s total), and the PR-body JSON retry at 17:40:31 (the built-in fallback recovered immediately; PR #17 exists).

One thing I could not inspect: the blob contents of the 140-run `fabro_runs_list` dump beyond its truncated preview, so the $0.0677 round is attributed by token delta (44,948 vs 11,465 input on the prior round) rather than an exact per-byte accounting.
