# Improve review — run 01M3201Q8GTC0EPT0S5YMK0Z32

- workflow: develop
- branch integrated: this revisor pass (unmerged until approved)
- status: succeeded (21.5 min, revisor pass — reason and cost in run detail)
- generated: 2026-09-21 13:10+0000 by revisor `fabro_ask`

---

All evidence gathered — run events, both agent journals, gate output, timings/cost from the conclusion event, and a full sweep of the tracker (10 seeds: `seeds-e218`, `seeds-facc`, `seeds-3791`, `seeds-48db`, `seeds-b291`, `seeds-cd75`, `seeds-9482`, `seeds-9fa3`, `seeds-69ae`, `seeds-436a`). Run baseline for impact sizing: 20m28s wall, $2.30 total, of which the implementer stage was 17m56s / $2.03 (88% of cost; 1,048s inference vs 27s tool time); planner 72s/$0.16; reviewer 51s/$0.11; gate green in 6.5s on a warm cache.

## Recommendations, ordered by expected impact

**1. Stop `dup-run-check.nu` from matching seed ids in full commit bodies — the run's single most dangerous near-miss.**
What happened: the preflight flagged `seeds-facc` as `duplicate` via commit `e307edf` (the *format-core* PR, whose squash body merely lists the seed id), the planner burned ~40s of its 72s adjudicating the false positive (journal painpoint), and the implementer then had to **override its own prompt's mechanical "route Blocked on `duplicate`, no judgment calls" rule** to proceed — a compliant implementer would have dead-ended a perfectly claimable seed and wasted the whole ~$2.3 run. Change: in `.fabro/scripts/dup-run-check.nu`, restrict the seed-id match to the commit subject line and `Fabro-Run:`/trailer lines, never the full body (optionally let planner-preflight pass a claimed-state override that downgrades foreign matches to advisory). Expected effect: false-positive `duplicate` verdicts disappear and the implementer's mechanical Blocked route becomes safe to follow.
*New-seed justification: no existing seed covers match scope — `seeds-69ae` (closed) fixed only the default `--base` branch.*

**2. Make `verify.nu` see untracked files — the mechanical verification lane silently skipped the test suite this run.**
What happened (implementer journal): `just verify implementer` derives touched crates from `git diff`, so untracked `tests/cli.rs` was invisible; it printed `test-file-touched=` empty, classified the crate code-touched-only, and **skipped the suite** — the implementer had to hand-run the full 47-test crate suite to compensate, inside the 18-minute stage. Every seed that adds new files (most seeds) hits this. Change: in `scripts/verify.nu`, derive touched paths from `git status --porcelain` (or the union with `git diff`) so `??` entries count. Expected effect: the test-file-touched class actually triggers the full-suite lane mechanically; no manual compensation.
*New-seed justification: `seeds-9482` (closed) fixed the `crates/<name>/**` regex and loud-failure arm, but explicitly still derives from the diff — untracked-file blindness is a different defect class.*

**3. Make the evidence capture pageable — the reviewer could not read the middle of its own evidence.**
What happened (reviewer journal): the 96.2 KB evidence blob is a single-line JSON string, so `read_file` offset/limit paging (which pages *lines*) could not reach the `main.rs` diff (~12.7k tokens); the reviewer fell back to reading the repo file — safe only because the worktree happened to be clean. On any bounce cycle with a dirty tree this routes Verification-blocked → a full evidence re-capture cycle. Change: `.fabro/workflows/develop/scripts/evidence.nu` should emit the capture with real newlines (pretty-printed, or one diff hunk per line). Expected effect: reviewers page any capture size directly; eliminates the repo-read fallback and a whole class of re-capture cycles.
*New-seed justification: no tracker seed covers evidence.nu output shape (the graph's `fabro-meta-c9f2` lineage addressed line caps, not single-line JSON).*

**4. Implement **`seeds-9fa3`** — the reviewer's binding style-guide rule stayed unauditable through a 2,567-line Rust approval.**
What happened: the reviewer session shows `rust-style-guide` discovered but `activated: []` and no `use_skill` in its tool allow-list (`read_file,grep,glob` only), while the prompt makes reading the guide a binding precondition for judging Rust diffs; whether the standards axis was applied to this run's large Rust diff is unverifiable from the events. Change (as the seed specifies, already user-approved 2026-09-21): in the `reviewer` node of `workflow.fabro`, either add `use_skill` to `tools` or exempt `.fabro/skills/**` from hiding for reviewer reads. Expected effect: the "read the guide first" rule becomes mechanically satisfiable and auditable; guide violations route Changes-requested instead of depending on model memory.

**5. Fix the lesson-capture contract — it is systematically unfulfillable in this repo.**
What happened (implementer journal): every `ml record` call fails with "No .mulch/ directory found. Run `mulch init`"; `lesson_capture` degraded to "skipped (ml unavailable)" even though a genuinely durable finding surfaced (sd 0.5.15 exits 1 on all error paths, not exit-0). A per-pass required answer now fails every run, wasting calls and losing lessons. Change: one-time `mulch init` in this repo (as its own loop-asset seed), or until then amend the Lesson-capture section of `.fabro/workflows/develop/prompts/implementer.md` to: "if `ml` fails for missing `.mulch/`, answer `lesson capture unavailable — no .mulch/` and journal once." Expected effect: the lesson channel either works or degrades cleanly and declared; no more guaranteed-failing calls each run.
*New-seed justification: no seed covers `.mulch` initialization — `seeds-3791` explicitly keeps "mulch CLI until the mulch phase" but is the sd→seeds cutover.*

**6. Planner: probe-verify reference-behavior claims before they enter the brief.**
What happened: the seed body's stale claim "exit-0-on-error behavior of the reference" was transcribed verbatim into `current_seed_brief`; the implementer had to empirically disprove it (live sd probes), ship a documented "MATERIAL SPEC CORRECTION" deviation, and the reviewer then had to adjudicate brief-vs-code divergence — extra probe cycles plus review risk, since a stricter reviewer could have bounced a correct implementation for "deviating from the brief." Change: in `.fabro/workflows/develop/prompts/planner.md` step 7, add one line: any brief bullet asserting observable runtime behavior of a named reference tool must be settled by the cheapest live probe at plan time (here: `sd show --bogus; echo $?`, ~200ms) or explicitly labeled "unverified claim (spec-transcribed)". Expected effect: stale behavioral claims stop reaching the implementer; eliminates mid-run spec corrections and the reviewer adjudication tax.
*New-seed justification: no existing seed covers brief claim verification — the planner-schema work (fabro-9ec3 lineage) banned only gate commands in briefs.*

Minor observations not rising to recommendations: the gate's own lint warns the toolchain pin `2026-04-14` is >45 days old (decision item: re-pin or silence, in `project-facts.md`); and `fabro_runs_list` returned `state: null` for this repo's PRs, which the planner let slide without the journaled degraded-mode note its prompt requires — worth folding into recommendation 6's planner-hygiene pass. Also, `seeds-3791` and `seeds-b291` are now unblocked by this run's `seeds-facc` close — the next runs have a clear queue.
