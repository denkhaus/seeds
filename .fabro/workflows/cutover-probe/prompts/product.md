Create the file `probe_product.txt` in the repository root with exactly this content (one line, trailing newline):

{"product_write":true}

Then reply with exactly one JSON object and nothing else:

{"outcome":"succeeded","preferred_next_label":"Product out"}
