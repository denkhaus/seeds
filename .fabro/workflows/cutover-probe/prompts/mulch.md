Create the file `.mulch/expertise/cutover-probe.jsonl` with exactly this content (one line, trailing newline):

{"probe":true}

Then reply with exactly one JSON object and nothing else:

{"outcome":"succeeded","preferred_next_label":"Write out"}
